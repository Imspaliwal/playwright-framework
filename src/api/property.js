async function updateProperty(connectionName, connectionType, propertyPayload) {
    // need default property payloads for all connecectors
    // form josn object for each property which want to add or remove
    // update the default property payload based on add or remove
    // pass the new payload with post call to update the property
}
